# =============================================================================
# Grafana Configuration - DEPRECATED
# =============================================================================
# This file is deprecated. All configuration has been moved to the root .env file.
#
# Migration Steps:
# 1. Run: make generate-secrets (from project root)
# 2. Or manually configure: PROJECT_ROOT/.env
#
# See: docs/guides/ENV-MIGRATION.md for details
# =============================================================================

# All Grafana configuration is now in: ../../.env
# Required variables:
#   - GRAFANA_ADMIN_USER (default: admin)
#   - GRAFANA_ADMIN_PASSWORD (must be set securely)
#
# To generate secure passwords:
#   make generate-secrets
#
# To validate configuration:
#   make validate-secrets
# GF_AUTH_GENERIC_OAUTH_AUTH_URL=...
# GF_AUTH_GENERIC_OAUTH_TOKEN_URL=...
# GF_AUTH_GENERIC_OAUTH_API_URL=...

# Database (optional; for multi-instance setups)
# GF_DATABASE_TYPE=postgres
# GF_DATABASE_HOST=postgres:5432
# GF_DATABASE_NAME=grafana_db
# GF_DATABASE_USER=grafana
# GF_DATABASE_PASSWORD=...

# Resource limits (optional)
# GF_MEMORY_LIMIT=512m
# GF_CPU_LIMIT=1

# Logging
GF_LOG_LEVEL=info
# GF_LOG_LEVEL=debug  # Use for troubleshooting

# Plugins
# GF_PLUGINS_ALLOW_UI_UPDATES=true

# Alerting (optional)
# GF_ALERTING_ENABLED=true
# GF_ALERTING_EXECUTE_ALERTS=true

# Feature toggles (optional; Grafana 7.0+)
# GF_FEATURE_TOGGLES_ENABLE=traceToMetrics,flameGraphPanel

# Reporting (optional)
# GF_REPORTING_ENABLED=true
# GF_REPORTING_RENDER_CONCURRENT_LIMIT=4

