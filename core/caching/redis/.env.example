# Redis Configuration (.env)
# Copy this file to .env and adjust values for your environment
# SECURITY: Do NOT commit this file to git. Add .env to .gitignore.

# Connection parameters
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=  # Leave empty for local dev; set for production
REDIS_DB=0

# Persistence
REDIS_PERSISTENCE_ENABLED=true
REDIS_PERSISTENCE_TYPE=rdb  # Options: rdb, aof, mixed

# Memory management
REDIS_MAXMEMORY=256mb
REDIS_MAXMEMORY_POLICY=allkeys-lru  # Options: noeviction, allkeys-lru, volatile-lru, etc.

# Replication (optional; for HA setups)
# REDIS_REPLICA_HOST=redis-replica
# REDIS_REPLICA_PORT=6379
# REDIS_REPLICATION_BACKLOG_SIZE=1mb

# Resource limits (optional)
# REDIS_MEMORY_LIMIT=512m
# REDIS_CPU_LIMIT=1

# Logging
REDIS_LOGLEVEL=notice
# REDIS_LOGLEVEL=debug  # Use for troubleshooting

# Cluster mode (optional; for distributed setups)
# REDIS_CLUSTER_ENABLED=no

